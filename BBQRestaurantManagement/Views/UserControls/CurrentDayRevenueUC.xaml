<UserControl x:Class="BBQRestaurantManagement.Views.UserControls.CurrentDayRevenueUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
               xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Background="White"
        FontFamily="{DynamicResource MaterialDesignFont}" 
             xmlns:local="clr-namespace:BBQRestaurantManagement.Views.UserControls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Margin="10" Grid.Column="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" >
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2"></DropShadowEffect>
                    </Border.Effect>
                    <Calendar SelectedDate="{Binding SelectedDay,UpdateSourceTrigger=PropertyChanged}"></Calendar>
                </Border>
                <Border Grid.Column="1" Margin="10" BorderBrush="#ad1457" BorderThickness="0.5">
                    <StackPanel Margin="10" VerticalAlignment="Center">
                        <Image Grid.Column="0" Height="100" Width="100" Source="/Pictures/FinancialProfit.png"></Image>
                        <TextBox Margin="10 20" Text="{Binding RevenueToDay,UpdateSourceTrigger=PropertyChanged,StringFormat='##,#'}"  
                            HorizontalAlignment="Center"
                            IsReadOnly="True"
                            FontSize="30"
                            Foreground="DeepPink"
                            materialDesign:TextFieldAssist.PrefixText="Revenue to day: "
                            materialDesign:TextFieldAssist.SuffixText="$"></TextBox>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <StackPanel Grid.Row="1" Height="50" Orientation="Horizontal">
            <materialDesign:PackIcon Kind="Clock"
                                     Foreground="Gray" 
                                     Margin="10 0"
                                     VerticalAlignment="Center"></materialDesign:PackIcon>
            <TextBlock Text="Lịch sử bán hàng trong ngày" 
                       FontSize="15"
                       FontWeight="Bold"
                       Foreground="Gray"
                       VerticalAlignment="Center"/>
        </StackPanel>
        <Border Margin="10" Grid.Row="2">
            <DataGrid CanUserAddRows="False" 
                Grid.Row="1"
                IsReadOnly="True"
                HorizontalAlignment="Center" 
                AutoGenerateColumns="True"
                Margin="0 20"
                ItemsSource="{Binding InvoicesHistory,UpdateSourceTrigger=PropertyChanged}">
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
